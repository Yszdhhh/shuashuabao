[pytest]
# 只收集本项目测试；references/ 下是第三方库自带用例（pywinauto 等），
# 裸跑 pytest 会因缺少其测试依赖产生 29 个 collection error。
testpaths = tests
norecursedirs = references .venv .venv-ocr build* dist* _old agent_out fixtures models
