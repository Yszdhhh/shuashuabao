# 09-22 晚测试安排：单人快测 → 夜间长线程蹭车

候选：主目录 `G:\刷刷宝\GameScript-Local`，分支 `fix/hitch-goal-archaeology-20260920`，HEAD `da566dd`（merge `b7ae0bb`）。
本轮新增：宝物负面 14 卡夹具；默认不拿 = 透支力量/贪婪献祭/金转木/杀敌梭哈/伐木契约/压制/诅咒之力/提高上限/木材梭哈 + pattern 命中（恶魔契约/玻璃大炮/金币梭哈/贪婪契约）；等级优势改为默认拿；UI 13 张全部可勾选放行；13 号计分修复。
机器独占：门禁 → 单人快测 → 夜间蹭车，串行，不并跑。

## 第一段：单人快测（现在，约 1 小时）

性质：**现有局内逻辑的基线**，证据等级 SOURCE_QUICK_TEST。动态调度尚未实现，本段结论不得记作“动态策略通过”。

入口：桌面「刷刷宝-快速测试.lnk」→ 点 UAC。窗口标题/启动输出应显示 HEAD `da566dd`、dirty 0。
观测：用户级 `SHUABAO_OBSERVE=1` 已开，无需设置。
设置：单刷模式，其余保持默认；宝物负面卡**全部不勾**（验默认不拿）。局数 3 局，时间允许可到 5 局。

Owner 现场只需要做：
1. 点 UAC 启动，开始运行后不操作。
2. 若卡住 >2 分钟无动作、或弹出异常框：截图，记下时间，然后停止（不要手动救局继续跑，否则该局作废）。
3. 若存档挑战 / 时光之穴列表 / 传家宝弹窗出现在画面上：用 PixPin 截整窗（新 Boss 素材，见下方“顺带采集”）。
4. 跑完 3 局后停止，告诉主架构“单人跑完了”。

主架构事后离线核对（不需要 Owner 盯）：

| 项 | 判据 | 数据来源 |
|---|---|---|
| A 闭环 | 每局开局→结束→回大厅→下一局，无人工介入；无 >90s 零输入卡死 | run.log / trace |
| B 宝物默认不拿 | 默认不拿的 13 张出现时一次都未被选中；等级优势出现时可被选中 | run.log 选卡记录 + 宝物面板帧 |
| C 局内基线 | 木材净囤积/峰值/高位时长、花费÷进账、F/G/其他决策占比、每次开 F 产出 | observe 目录 + `tools/summarize_observe.py` |
| D 羁绊/技能 | 羁绊确认获得数、G 积压峰值与是否触发 ≥8 抢占、主线停滞段 | run.log + observe |
| E 战后 Boss | 列表不空滚；双帧无卡时记 unconfirmed，不写“受理”；有无掉落/战果画面（原帧目视） | trace + frames |

注意（已知口径）：SELECT_SLOT 是决策数不是点击数；点击 ok≠获得；余额差≠收入；读不出记 null 不记 0。

## 第二段：夜间长线程蹭车（单人跑完、主架构同步 SHA 后）

入口：桌面「刷刷宝 实机测试台.lnk」（主架构会先把 SHA 更新为 da566dd 并读回）→ UAC → 13。
参数（快捷方式内已固定）：ROUNDS=30、DURATION_S=36000（10h 上限）、CAPTURE_ROOT=G:\刷刷宝\captures。
不逐局盯盘，跑完或卡住再看。

本轮首次实机验证：
1. `0f70601` 房主选难度页超时 → 语义 Esc → 确认/回房 → 拉黑该房继续搜房（不得提前转考古）。若 Esc 无可见变化须零输入 + BLOCKED，不能把发送当成功。
2. Boss 双帧无卡 → done/unconfirmed，不空滚。
3. 计分：30 局达标 + 考古交接确认才 PASS；考古未确认显示 NOT_OBSERVED（不再是 NOT_REQUIRED）。
4. 宝物默认不拿名单在蹭车局内同样生效。

已知限制：蹭车时门票余额恒 None，“门票耗尽转考古”不会触发，终点只由 30 局决定。

## 顺带采集（新 Boss，Owner 手动截图）

竞品 1.6.2 显示：传家宝新增 21界龟；时光之穴新增 55吞咽者布鲁 / 56狩猎者阿娅米斯 / 57无疤者奥斯里安。我方 catalog 缺这 4 个，且传家宝末项错标“54莫阿姆”。
需要：存档 8 格、时光之穴列表（滚到底）、传家宝弹窗（滚到底）的整窗截图，含是否已解锁/已挑战态。截图放 `G:\刷刷宝\captures\new_boss_20260922\`。竞品图不能当我方模板。

## 之后

单人基线数据回来后，主架构据 `DYNAMIC_SOLO_ARCHITECTURE_V1.md` §11 给唯一 L1 实施 agent 第一批任务（先影子模式，不改线上决策）。
