# Boss 中间回传后的线程安排

主架构负责复杂机制/动态调度契约、跨层审查、主目录及快速入口、合并与发布验收。外部 agent 不负责拍定未知机制或新增策略阈值。Owner 派发以下提示词；本文件未自动向任何线程发消息。

## 线程 1：原 Boss agent，定点原帧收口

你这次中间交付 HOLD，需要修正统计和补最小原帧核对。此前“只看已定位异常旁原帧”允许这一步，不是禁止看帧。

主架构已独立核对 trace（证据根 `G:\刷刷宝\captures\hitch_lobby_chain_20260922_004851_422384`）：
- click:18瑟莱德丝公主：tick 403/713/1292/1586/1894/2450/2778/3265/3651/4056，均 ok=true。
- 传家宝兜底：tick 415/1306/1598/1906/2462/3277，共 6 次。
- click:17年兽：tick 725 ok=true，4081 ok=false；两处 post_confirm 为空。

因此先更正：目标点击被接受 1、兜底点击 6、你报告中的未点目标 2、目标点击被拒绝 1；不能写“成功1/跳过2/兜底7/失败1”。没有业务后置的 click ok 不能称挑战成功。BossNotUnlockedLast 是分支原因，不证明未解锁；兜底点击是否违反当时已批准策略须核对，先不要直接定为错点。

样本暂称“10 个战后事件段”，保留 game_count 原值；未解释 0/1/3/.../10 与 round_outcome 的关系前，不称完整 10 局。

第一批只做 3 个样本，每个最多 4 张原帧，总计最多 12 张：
1. tick 403 后的时光之穴异常；
2. tick 2778 后的空滚变体；
3. tick 725 前后的 17 年兽。
通过 manifest 的实际映射找 action_before/action_after；tick 不是帧文件序号，禁止猜 f0403。已有文件直接看，不新抽整录屏、不跑全量 OCR、不改代码。每样本区分：目标是否看见、点击是否被接受、列表是否消失、掉落/挑战是否确认、仍未知什么。帧缺失写确切预期路径或缺失映射。

先输出独立文件 `G:\刷刷宝\_facts_20260922\mechanics_solo\BOSS_HISTORICAL_RECHECK.md`，不要只引用旧报告。下一次回传交这 3 个样本及更正表即可；其余事件列后续，不阻塞当前包。无须先等 Owner 解锁截图。你不是唯一执行者，不覆盖其他人文件。

## 线程 2：当前打包 agent，保持同 SHA 收口

继续现有 9ed8b52979f4192de9b3a3ac90e3974a9ae3eeec 的构建，不启动第二套全量测试。你独占重型计算。当前 retry 日志停在 pytest 不说明卡死；下一次回报请给 PID/实际进度或子步骤、OCR 路径修正后的结果、剩余项，完成后给完整产物三元组与日志。

UI 身份补充已独立核对：主目录及打包树源码 index.html SHA256 为 d7f49253ab9fcfedc19302c8b91231abc29e7e90df184659a8f0d0d91ef67881；UI_BUILD 已是 UI-22，来源 commit 6a4bba4 名称误写 UI-21。当前构建 index SHA256 为 da35a4a730aba2a2f88dbe9118e9f8403a5f802bfa15b216092944275f0851b5。不要重复移植 UI 或回退旧分支。

先完成原授权的 -NoDeploy 包、完整门禁、双 harness、TLS/签名/UI 同次构建核验。不能把候选产物 PASS 写成桌面已更新；桌面 current.json 仍 bc228b。保持既有正式切换/Grok/快捷方式授权边界。动态策略尚未实现，不进此包。

## 线程 3：原 UI 设计 agent，确认唯一参考

仅交付最终 UI-22 设计的 source index.html SHA256、备份绝对路径和完整验收词；已有最终参考直接提供，不重做设计、不热改 dist、不直接改主目录。

主目录当前已提交文件 SHA256：d7f49253ab9fcfedc19302c8b91231abc29e7e90df184659a8f0d0d91ef67881。先明确它是否就是你最后确认的 UI-22；不同则列差异并保全你的原件。相同则不要再要求接手者重新提交。

验收列标题/过场/预检/完成/激活/小窗每项的预期，区分有 main.ts+宿主的真实联调与纯预览。不能以 UI-22 标签代替版本一致。沿用“所有 UI 改动进入源码和 commit”的原则。

## 线程 4：原机制/素材 agent，仅核对动态信号

执行 DYNAMIC_SOLO_EVIDENCE_AND_BOSS_CLOSEOUT_20260922.md 的机制部分；架构由主架构负责，不再产出另一套阈值方案。新架构见 `G:\刷刷宝\_facts_20260922\solo_strategy\DYNAMIC_SOLO_ARCHITECTURE_V1.md`，尤其第11节。

第一轮只交现有证据的紧凑矩阵：主线失败/X-Y、资源与角标、TAB entry/mid_run、confirmed 持有卡、动作成功后置。每项 producer/consumer 的当前 file:line、实际原帧/trace、n、新鲜度、缺失行为、可否接入。TASK_H 基线找不到就 NOT_FOUND。禁止把“只存在字段”判为“有效接入”。

本轮只读，小范围查已有文件，不批量 OCR、不新开实机、不改生产代码。输出 `G:\刷刷宝\_facts_20260922\solo_strategy\DYNAMIC_SIGNAL_EVIDENCE.md`，先交矩阵，再列缺口。

## 主架构本轮已完成及后续

- 已独立复核 Boss trace 点击记录和证据口径；报告 HOLD 不回滚已通过离线验收的 Boss 修复。
- 已完成动态架构第11节：纯决策输入输出、部分排序、按事件失效、事务边界让位、币种预算、12 个确定性验收场景。设计完成不等于运行实现 PASS。
- 等信号矩阵核实后，主架构给唯一 L1 agent 首批影子实现任务，不让它自选机制或复制业务运行时。
- UI 完整参考、构建验收由两条外部线并行补齐；正式桌面仍 HOLD，快速测试正常 UAC 路径由主架构收口。
- 所有全量门禁/打包/实机按机器独占串行。原帧浏览、文档证据核验可并行；本次不新开全量测试、不启动游戏。
