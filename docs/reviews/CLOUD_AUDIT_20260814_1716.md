# 云端改动审计 · 2026-08-14 17:16

角色：板块 6 只读质检。未改业务代码、未改快照、未 force-push、未合并 PR。

依据：`AGENTS.md` → `docs/handoff_20260814/06_AGENT_CLOUD_AUDIT.md` → `docs/CONTRIBUTING_GATE.md` → `docs/AGENT_CORRECTION_GATES_20260811.md`。

## 1. 审计范围

用户提示词里的 `<起点>..<终点>` 未填。仓库内无既有 `CLOUD_AUDIT_*.md`，按 06 默认「上次审计之后的新 commit」处理。

| 段 | 范围 | 说明 |
|---|---|---|
| A. 已推送 | `886b7aa..origin/trial-merge`（`2cc5c89`） | 发版门禁落地之后、远端 `trial-merge` 上的纪律时代。`origin/trial-merge` **仍停在** `2cc5c89`（2026-08-12 19:14）。 |
| B. infra 刚完成、未推送 | `2cc5c89..infra-layering`（`6d89dda`） | 用户 17:08 称「infra 现在才刚执行完」。本地 `infra-layering` 在 `6d89dda`；本地 `trial-merge` 停在 `0c52944`（只含 shell P0，不含后续 L0/L1/modes）。 |

本报告覆盖 A+B 共 40 个 commit（含 4 个 merge）。A 段已在 GitHub；B 段只在本地 worktree `GameScript-InfraB`，**还不是 `origin/trial-merge`**。

图：

```text
886b7aa (pre-gate L1) 
   └── dd2b486 … 2cc5c89     == origin/trial-merge   [段 A]
                              └── 1d8f101 … 6c3730c
                                   ├── 0c52944          == 本地 trial-merge（shell P0）
                                   └── c2204cf … 519d9d4
                                        └── db95189 (merge) ── 6d89dda  [段 B]
```

## 2. 逐 commit 结论

判定只看该 commit 自己的 tree，不因后续「修回来了」改写历史结论。

### 段 A · `886b7aa..2cc5c89`（已推送）

| SHA | 标题 | 总评 | 一句话 |
|---|---|---|---|
| `dd2b486` | test: add release gate and L0 lobby contract suite | **PASS** | 引入门禁 + C1–C4，单层（测试/文档/工具）。 |
| `7db2be5` | test(fixtures): repair three stale frozen replay expectations | **PASS** | 先改夹具期望、再改快照；`reason` 写明「修期望不是降门槛」。 |
| `31daf64` | docs: add AGENTS.md | **PASS** | 纯文档。 |
| `ba29936` | test: let each test file run standalone | **PASS** | 只加 `tests/conftest.py`。 |
| `e18e37a` | feat(policy): rarity-first presets… | **PASS** | 只动 `choice_policy` + 测试。 |
| `d7612df` | test: contract-lock the four choice semantics | **PASS** | 契约 + 计划 + `release_gate.py` 纳入契约。 |
| `f7717fc` | feat(policy): confirm the six negative treasures | **WARN** | 策略层为主，顺手改了 `settings.py` / 新建 `skill_meta.json`。 |
| `4e7edbb` | docs: split the wiring handoff | **PASS** | 纯文档。 |
| `a3b5241` | feat(panel): rebuild the control panel | **PASS** | 外壳。 |
| `b147bb5` | fix(panel): negative-treasure group title | **PASS** | 外壳。 |
| `2067952` | chore(fixtures): pack negative-treasure frames | **PASS** | 纯夹具。 |
| `bcb3f48` | feat(l1): wire choice_policy into mediator | **PASS** | mediator hunk 均在选卡/面板 FSM；C2 同步了 `_choice_session` / `_choice_policy_idle`。 |
| `82cd732` | test(contract): lock treasure_negative descriptions | **PASS** | 契约。 |
| `7f642f7` | test: skip OCR/Windows/raw-fixture when assets absent | **WARN** | 为云端可收集加 skip，本身正当；与后一条叠加后被用来下调基线。 |
| `122b35b` | feat(perception): card template bidirectional asserts | **PASS** | 感知/词典。 |
| `11a9796` | feat: rename the shipped app to 刷刷宝 | **WARN** | 外壳为主，`mediator.py` 改了一行提权提示文案（`@@ -5822`）。 |
| `21f0528` | test: make Linux/cloud gate collectable and refresh baseline | **FAIL** | 用云端 `passed: 462` 把快照从 481 下调。见 §3.1。 |
| `f1002cc` | fix(build): run the gate with the dev interpreter | **PASS** | 只改 `build_release.ps1`。 |
| `ed40ed5` | Merge pr/1 | **WARN** | merge，无独立 diff。 |
| `b137877` | Merge pr/2 | **WARN** | merge。 |
| `a4517a4` | Merge pr/3 | **WARN** | merge。 |
| `a627910` | fix(tools): decode card fixtures… rebaseline on Windows | **PASS** | 把 pytest 基线从 462 **抬回** 537，commit 正文明确拒绝云端数字。 |
| `c8f3bcb` | docs: finish the rename in comments… | **WARN** | 文案/路径跨外壳、mediator、工具、UI；无行为。顺手去掉工具里的本机绝对路径。 |
| `17ece0e` | feat(shell): 刷刷宝 V0.2 | **PASS** | 外壳 + 打包。 |
| `52df5b7` | feat(l0): 英雄模式六阵营 FactionSpec | **WARN** | mediator 只动英雄弹窗（L0），但同一 commit 塞进 lobby 模板 + 整包 `fixtures/hero_modal_20260812_180825/`。 |
| `2cc5c89` | feat(config): 技能预设扩充、习惯权重 schema 与调研交接 | **FAIL** | 一层提交里混了策略、新模块、调研文档、抽帧工具、夹具契约。见 §3.2。 |

### 段 B · `2cc5c89..6d89dda`（infra 刚完成，未推送）

| SHA | 标题 | 总评 | 一句话 |
|---|---|---|---|
| `1d8f101` | feat(shell): replace hidden dry-run with visible learning mode | **FAIL** | 外壳 + L0 `_request_create_room` + L1 选卡观测；`Settings.dry_run` 默认 `True→False`，段 B 头上 6 条测试因此变红。见 §3.3。 |
| `6f22d88` | chore(release): bump to V0.3 | **WARN** | 版本号 + 同步脚本 + handoff，层可接受但动机是发版不是单一层。 |
| `cb7df0c` | docs: point desktop sync at sync_to_desktop.ps1 | **PASS** | 文档/脚本指针。 |
| `656a7be` | chore: ignore pytest cache and agent tmp | **PASS** | 只改 `.gitignore`。 |
| `6c3730c` | test: add 20260814 hitch/longtest/stage evidence fixtures | **WARN** | 夹具为主，同时改了生产模板 `bond_hide_btn.png` / `bond_refresh_btn.png` / `stage_region_jiushidalu_unselected.png`。 |
| `c2204cf` | feat(config): add official KB, skill catalogs, and lexicon loaders | **FAIL** | 知识库 JSON + `skill_catalog`/`bond_capacity` + `choice_ocr.py` + `mode_specs.json` + 审计工具。见 §3.4。 |
| `9515b9c` | docs: add 20260814 research notes and five-plate handoff | **PASS** | 纯文档。 |
| `0c52944` | feat(shell): land control center P0 with ModeSpec gate | **WARN** | 外壳 `src/gamescript/shell/` 干净，但再次新增 `config/mode_specs.json` + `official_strategy_defaults.json`（与 `c2204cf` 内容相同、平行分支各加一份）。 |
| `77fb8b9` | feat(l1): skill-point-first pick policy and lab focus rotation | **FAIL** | L1 大包 + 战后大秘境/暂停退出 + 调用尚不存在的 `_is_in_game_hud(..., strong_only=True)`；C2 漏字段。见 §3.5。 |
| `1819dac` | feat(l0): confirm stage by highlight and old-world region card | **FAIL** | L0 + `stage_selector` + `scenes.json` + 冻结夹具 + `GATE_BASELINE` 同提交。见 §3.6。 |
| `6ea5f9e` | feat(modes): add read-only ModeSpec skeleton and RuntimeStatus | **PASS** | 模式骨架单层。 |
| `519d9d4` | docs: record infra A/B layering status | **PASS** | handoff。 |
| `db95189` | merge shell P0 into infra-layering | **WARN** | 把 `0c52944` 并进 infra 线。 |
| `6d89dda` | docs: keep newest plate status notes at the top | **PASS** | handoff 置顶。 |

## 3. FAIL 详情

### 3.1 `21f0528` — 用云端数字下调 `GATE_BASELINE`（清单 3）

**违反：** 06「云端测试数永远不能成为下调基线的依据」；`CONTRIBUTING_GATE`「禁止为了让门禁变绿而更新快照」。

**证据：**

```diff
# 21f0528 docs/baselines/GATE_BASELINE.json
-        "passed": 481,
+        "passed": 462,
+        "skipped": 11
```

commit message：`Refresh GATE_BASELINE to the observed Linux reachable counts`。JSON `reason` 写的是 Linux/cloud 缺 UIA/OCR/official_raw。

**后续：** `a627910` 把数字抬回 537，并写明「云端 462 不作基线」。历史结论仍是 FAIL——中间窗口里门禁少看 19 条。

**建议拆分/回滚：** 已由 `a627910` 纠正，不必回滚。以后云端 PR 只许加 skip/shim，不许改 `stages.pytest.observed.passed` 向下。

**归属：** 板块 5（门禁/基建）；云端 PR #3 作者。

### 3.2 `2cc5c89` — 混层（清单 1）

**违反：** 一个 commit 只动一层。

**证据（`--stat`）：**

| 路径 | 层 |
|---|---|
| `config/choice_policy.json`、`src/gamescript/choice_policy.py`、`config/skill_meta.json` | L1 / 策略 |
| `src/gamescript/habit_preference.py`、`config/habit_preference.schema.json` | 新模块 |
| `src/gamescript/licensing.py` | 新模块 |
| `docs/research/*`、`docs/CURRENT_STATUS_AND_HANDOFF_20260812.md` | 文档 |
| `tools/video_breakdown.py` | 工具 |
| `fixtures/treasure_negative/DESCRIPTIONS.json` + 契约测试 | 夹具/契约 |

**建议拆分：** ① 策略/skill_meta；② habit_preference；③ licensing stub；④ 调研文档；⑤ video_breakdown。已在远端，不回滚，下次禁止再捆。

**归属：** 板块 3（策略/KB）+ 板块 5（不该让调研/授权 stub 进同一提交）。

### 3.3 `1d8f101` — 外壳/L0/L1 混交 + `dry_run` 默认翻转（清单 1、3、6）

**违反：** 分层纯度；行为变更后测试未跟上。

**证据 A — mediator 同时改 L1 与 L0：**

```diff
# L1 选卡
@@ -2064,6 +2065,29 @@ class Mediator:
+        if self.settings.dry_run:
+            append_learning_observation({ "event": "choice_decision", ... })

# L0 建房
@@ -4276,7 +4300,42 @@ class Mediator:
     def _request_create_room(...):
+        if self.settings.dry_run:
+            self.act_click(candidate, "CreateRoom-open")
+            ...
+            return LoopAction.Continue
```

同一 commit 还改 `desktop_app.py`、`ui/src/*`、`config/default_settings.json`、`settings.py`。

**证据 B — 默认值：**

```text
origin/2cc5c89  settings.py  dry_run: bool = True
infra/1d8f101   settings.py  dry_run: bool = False
```

**证据 C — 段 B 头上 6 红 vs 原点绿（本机 Windows，非提权；数字只作参考，不作基线）：**

| 用例 | `2cc5c89` | `6d89dda` |
|---|---|---|
| `CadenceTests::test_run_loop_sleeps_cadence_minus_elapsed` | PASS | FAIL（`run()` 因未提权进 ERROR） |
| `ReviewFixTests::test_loading_cadence_not_compressed_by_loop_sleep_ms` | PASS | FAIL（同上） |
| `P0SecurityFoundationTests::test_mediator_fill_room_dialog_uses_executor` | PASS | FAIL（UIPI 拒输入） |
| `P0SecurityFoundationTests::test_mediator_stage_scroll_uses_executor` | PASS | FAIL（UIPI 拒输入） |
| `S0PanelFsmTests::test_panel_waits_for_visibility_before_close` | PASS | FAIL（`OpenSkillPanel` 不在点击列表） |
| `TemporalSameRoomLoopTests::test_create_room_window_blip_does_not_error_early` | PASS | FAIL（`CREATE_ROOM` 30s 后 `Break`，期望 `Continue`） |

`6d89dda` 的 handoff 把这 6 条写成「e997b39 遗留」。与上表矛盾：它们在 `2cc5c89` 全绿。更像是 `dry_run` 默认 False 后，依赖「默认观察模式」的用例开始走真输入/真停机。

**建议拆分：** ① 外壳改名「学习模式」+ UI；② L0 建房在 dry_run 下不进 pending；③ L1 观测写入。默认值翻转必须同步改测试 `Settings(dry_run=True)` 或 mock 提权，**不要改快照**。

**归属：** 板块 1（学习模式/外壳）主责；板块 5 误判「预存债」需改 handoff 表述。

### 3.4 `c2204cf` — 知识库 / 新模块 / 感知 / 模式骨架混交（清单 1）

**违反：** 分层纯度。

**证据（`--name-status`）：**

- 知识库：`config/game_mechanics_kb.json`、`skill_card_catalog.json`、`skill_card_rarity.json`、`skill_archive_unlocks.json`、`bond_stack_catalog.json`、`official_strategy_defaults.json`
- 模式骨架：`config/mode_specs.json`（与 `0c52944` 内容相同，平行各加一次）
- 新模块：`src/gamescript/skill_catalog.py`、`bond_capacity.py`
- 感知：`src/gamescript/vision/choice_ocr.py`、`config/choice_lexicon.json`
- 工具：`tools/bond_knowledge_audit.py`、`merge_skill_archive_screenshots.py`、`scan_longtest_traces.py`

未回写 `CURRENT_STATUS_AND_HANDOFF_20260812.md`（清单 5 → WARN，并入本条）。

**建议拆分：** ① 只读 JSON 目录；② `skill_catalog`/`bond_capacity` + 一致性测试；③ lexicon/OCR；④ `mode_specs.json` 留给 `6ea5f9e`；⑤ 扫描工具。

**归属：** 板块 3（KB/目录）；`mode_specs.json` 应归板块 1 / 模式骨架，不要由 KB commit 顺手落地。

### 3.5 `77fb8b9` — L1 超大包、跨段、C2 不全、中间 commit 不可运行（清单 1、4）

**违反：** 分层纯度（L1 + 恢复/战后 + L0 边界调用）；C2 新增字段未全部进 `INGAME_POLLUTION`；该 SHA 单独检出时 MAIN_LINE 会 `TypeError`。

**证据 A — 调用尚未存在的关键字参数：**

```text
# 77fb8b9 的定义
def _is_in_game_hud(self, frame: Frame) -> bool:

# 77fb8b9 的调用（src/gamescript/mediator.py，约原 6132 行）
if self._find_stage_page(frame) and not self._is_in_game_hud(frame, strong_only=True):
```

`strong_only` 是子提交 `1819dac` 才加到签名上的。`77fb8b9` 单独 bisect / checkout 会在「MAIN_LINE 看见选关页」路径上炸。

**证据 B — 同一 commit 还改战后/辅助框：**

```diff
@@ -5774  PAUSED + lab_immediate_quit → Phase.QUIT
@@ -5928  大秘境确认：取消耗尽不再 ERROR，改为跳过
@@ -6106  _lab_try_reenter_quit
```

这是「恢复与战后」，不是 L1 选卡。

**证据 C — C2 同步不完整：**

`77fb8b9` 往 `INGAME_POLLUTION` 加了：

```python
"_lab_skip_great_rift_until", "_lab_bond_picks", "_skill_owned", "_bond_confirm_key"
```

同 commit 新增大厅隔离清单里**没有**的局内字段：

```python
self._bond_icon_cache
self._last_skill_panel
self._last_bond_attempt
self._last_treasure_attempt
```

契约在字段「清单有、对象无」时会红；「对象有、清单无」会静默漏污染。06 要求新增即同步。

**证据 D — 未回写 handoff**（清单 5）。行为大改只在后两个 docs commit 里被概括。

**建议拆分：** ① 纯 `choice_policy` / 技能点优先；② 羁绊双帧/图标学习；③ `lab_focus` + `tools/lab_run.py`；④ 战后大秘境/暂停退出；⑤ 完整 C2 清单。`strong_only=` 必须和签名改动在同一 L0 commit，或 L1 先不要传这个参数。

**归属：** 板块 3（拿卡算法）+ 板块 5-D（lab 轮转/战后）。L1 车道本应串行，这个 commit 把两家的活捆在一起。

### 3.6 `1819dac` — L0 + 感知 + 冻结夹具 + 门禁快照（清单 1、3）

**违反：** 分层纯度；快照与行为、夹具同提交。commit message **不含** `--reason`（JSON 里有 `reason` 字段）。

**证据：**

```text
M  config/scenes.json                          # 感知/L0：stage_start 去掉 stage_begin_btn/startChallenge
M  src/gamescript/mediator.py                  # L0：_find_old_world_unselected / 高亮门闩
M  src/gamescript/vision/stage_selector.py     # 感知
M  fixtures/.../stage_select_scroll/case.json  # 冻结期望：补 stage_targets=["1-8"]
M  docs/baselines/GATE_BASELINE.json           # scene_templates ok 131→130
M  tests/test_hero_mode_temporal.py            # 断言从「点击 StageStart」改为「无高亮不得点开始」
M  tests/test_temporal_same_room_loop.py
```

`stage_start` 去插画/挑战模板、高亮 fail-closed，方向与红线一致（少误点），**不是**用合成帧冒充实机。问题是层和快照捆在一起。

`test_stage_selector.py` 有一处去掉 `mock_click.assert_not_called()`，只留 `Continue`——单测收口变弱（清单 3 → 记在本条，不另开 FAIL）。

**建议拆分：** ① `scenes.json` + `stage_selector` + mediator L0 + 单测；② 冻结 `case.json`；③ `python tools/release_gate.py --update-baseline --reason "stage_start 去插画后模板数 131→130"`。

**归属：** 板块 5-D1/选关（L0）+ 感知。快照更新应留在板块 5 门禁流程，不要塞进功能 commit。

## 4. 红线扫描（清单 2）— 在 `6d89dda` 树上

| 红线 | 结果 | 证据 |
|---|---|---|
| `room_start` / `map_create_room` 的 `fallback` 恒 `null` | **PASS** | `config/scenes.json` L49、L130 均为 `null`。`create_room_confirm` 仍是 `blue_bottom_dialog_roi`（C4 不管这条，历史如此）。 |
| `mode_specs.json` 除 `normal_farm` / `lab` 外 `live_enabled=true` | **PASS** | `follow_team`/`gambling_wood`/`raid_wait`/`lobby_hitch` 均为 `false`；只有 `normal_farm`、`lab` 为 `true`。 |
| `-zs` / `F4` / 压力转移接入自动点击 | **PASS** | `src/gamescript/modes/flows.py` 仅注释「yalizhuanyi.png … mediator 无路径」。`mediator.py` 无 `yalizhuanyi`/`-zs`/`VK_F4`。 |
| `quick_join` / 颜色兜底获得点击权 | **PASS** | 出现处是 `forbidden_actions` 与「拒绝点击快速加入」日志（`mediator.py` 约 5427、5431），不是授权。 |
| 合成帧冒充实机已验证 | **PASS** | 断线场景仍 `BLOCKED`；`7db2be5` / 现行快照写明不得合成。 |

段 A 的 `e997b39`（范围外、历史混层 + 一度把 `room_start.fallback` 写进同一提交）仍在 `trial-merge` 祖先里。本次范围内**没有**再给进房/建房加颜色兜底。

## 5. 契约 / 卫生 / 可跑验证

### 5.1 C2（清单 4）

- `bcb3f48`：同步了 `_choice_session`、`_choice_policy_idle` → 合格。
- `77fb8b9`：同步了 4 个 lab/bond 字段，漏 4 个（§3.5）→ **FAIL**。
- `1d8f101`：未新增实例字段，C2 无义务。

`choice_policy.json` 负面/必拿名单：外壳 `0c52944` 仍走 `treasure_allow_negative` / EX 只读展示；`tests/test_desktop_app.py`、`tests/test_shell_progress.py` 仍在。未看到删契约。

### 5.2 卫生（清单 5）

| 项 | 结果 |
|---|---|
| `debug-5f5f6a` / `#region agent log` | **WARN**。`6d89dda` 的 `src/` 已搜不到。handoff 写「只在工作区删了，未单独 commit」——清理没有独立 SHA，无法证明哪一次带走。 |
| 新增本机绝对路径 | **PASS**（`c8f3bcb` 还拆掉了工具里的个人路径）。 |
| 行为 commit 回写 handoff | **WARN**：`c2204cf` / `77fb8b9` / `1819dac` 当时没写；`519d9d4`、`6d89dda` 后补。`6d89dda` 把 6 条红测试误标成 e997b39 遗留，需改表述（只改文档，勿改快照）。 |
| commit message 说清层与动机 | 段 B 的 `feat(l0)`/`feat(l1)`/`feat(config)` 标题清楚，但实际文件比标题宽。 |

`6c3730c` 改的 `bond_hide_btn.png` / `bond_refresh_btn.png` 在 `validate_scenes.py` 里是 `root_unreferenced`（未进 `scenes.json`）。生产图和证据夹具捆在一起，归属板块 5。

### 5.3 可跑验证（清单 6）— **本机 Windows 满资产、非提权；数字只作参考，不作基线**

在 `6d89dda`（`C:\Users\10639\Desktop\GameScript-InfraB`）跑：

| 命令 | 结果 | 备注 |
|---|---|---|
| `pytest tests/contract tests/test_choice_policy.py tests/test_skill_knowledge_consistency.py tests/test_mode_specs.py tests/test_skill_catalog.py tests/test_official_strategy_defaults.py tests/test_skill_rarity.py -q` | **179 passed** | 06 点名的纯函数/配置/契约，全绿。 |
| `pytest tests -q` | **6 failed, 818 passed, 11 skipped, 2 xfailed** | 6 红见 §3.3；原点 `2cc5c89` 上这 6 条全绿。 |
| `python tools/validate_scenes.py` | `ok=130 missing=0` | 与现行快照一致。 |
| `python tools/run_frozen_replay.py --check` | 6 PASS + 1 BLOCKED | 与快照一致；断线仍 BLOCKED。 |
| `python tools/release_gate.py` | 未完整跑通 | pytest 阶段会因上述 6 红失败。按规矩：**不要为此改快照**。 |

`GATE_BASELINE.json` 仍写 `pytest.passed=537`。818 > 537，「不得低于」这一侧没被下调；真正挡门禁的是 6 个 failed，不是数字大小。

云端 Linux 仍会 skip UIA/OCR/official_raw（`7f642f7`+`21f0528` 的 conftest）。**不要把云端通过数写回 537。**

## 6. 趋势观察

1. **分层比 8-12 的 `e997b39` 有进步，但段 B 又开始捆。** infra 的 B1–B6 标题按层切开了（`656a7be`/`6c3730c`/`c2204cf`/`9515b9c`/`77fb8b9`/`1819dac`/`6ea5f9e`），可是 `c2204cf` 和 `77fb8b9` 内部仍然跨层；`1d8f101`（板块 1，早于 infra 独占窗口）是教科书式 L0+L1+外壳。
2. **快照纪律两极。** `7db2be5`、`a627910` 写清「为什么改快照、为什么不是改代码」。`21f0528` 用云端 462 下调是本轮唯一「数字当基线」FAIL，已被抬回。`1819dac` 把 `131→130` 塞进功能提交，差在手续。
3. **脏文件从「126 个未提交」变成「已分层但未推送」。** `origin/trial-merge` 仍是 `2cc5c89`。本地 `trial-merge`=`0c52944`，`infra-layering`=`6d89dda`。往 origin 推之前应先消化 §3.3 的 6 条红（改测试或恢复测试用 `dry_run=True`），**禁止** `--update-baseline` 把 6 FAIL 写进快照。
4. **C2 又出现「加字段漏清单」。** `bcb3f48` 做对了；`77fb8b9` 做了一半。这是改 `mediator.py` 局内状态时最容易踩的回归。
5. **`mode_specs.json` 加了两次**（`c2204cf` 与 `0c52944` 内容相同）。合并没炸，但说明板块 1 与板块 5 没有先约定文件所有权。
6. **测试数：** 原点纪律时代快照 537；段 B 头上本机 818 passed / 6 failed。增量来自新单测，不是基线被抬。6 红是新债，不是 e997b39。

## 7. 建议领走（只报告，本 agent 不修）

| 问题 | 建议归属 |
|---|---|
| 6 条 pytest 红（`dry_run` 默认 False / 未提权） | 板块 1 补测试夹具；板块 5 改正 handoff「e997b39 遗留」表述 |
| `77fb8b9` C2 漏字段；`strong_only` 跨 commit | 板块 3 补 `INGAME_POLLUTION`；板块 5 把签名与调用收进同一 L0 commit（若尚未 push，可拆；已 push 则跟 commit） |
| `c2204cf` / `2cc5c89` 混层（已落盘） | 不回滚；板块 5 在后续 PR 说明「已发生、不再仿」 |
| `1819dac` 快照手续 | 板块 5：下次功能与 `--update-baseline --reason` 分开 |
| `debug-5f5f6a` 无独立清理 SHA | 板块 5：若还要证明 A 步，补一个只删残留的 commit（若工作区已干净则在 handoff 写「随 B 提交带出，无独立 SHA」即可） |
| 未推送的段 B | 整合方：先处理 6 红再 push `infra-layering` → `trial-merge`。本审计 PR **只含本报告**，不要当业务合并车。 |

下一轮审计默认范围：`6d89dda`（或当时的 `origin/trial-merge`）之后的新 commit。
