# 抽帧单 · 13 号长测 选关/选卡（20260814_195647）

把下面整段复制给新会话。本单做完即弃。判定归板块 7，拆解员只交证据。

```text
你是一次性抽帧拆解员。工作目录：G:\刷刷宝\GameScript-Local
先读 AGENTS.md 的红线（不改行为代码、合成帧不冒充实机证据），然后只做本单，做完即收工。

【素材】
录屏：C:\Users\10639\Desktop\录屏素材\20260814_195647.mp4
对应 trace（按时间顺序）：
- C:\Users\10639\AppData\Local\ShuaBao\20260814\trace_lab_20260814_195653.jsonl  （建房短失败）
- C:\Users\10639\AppData\Local\ShuaBao\20260814\trace_lab_20260814_195723.jsonl  （建房后 hwnd=None，ERROR unhealthy frame timeout）
- C:\Users\10639\AppData\Local\ShuaBao\20260814\trace_lab_20260814_200601.jsonl  （主跑：4 局全 TIMEOUT，约 20:06–21:10）
incidents：当天 %LocalAppData%\ShuaBao\20260814\ 下未见到 incidents 目录；若抽帧时出现再一并收。
录屏与 trace 的对齐基准：录屏 00:00 ≈ 2026-08-14 19:56:47。
- 195723 tick1 ≈ 录屏 +38s（19:57:25）
- 200601 tick1 ≈ 录屏 +555s（20:06:02）
核对方法：录屏 +555s 应是已在局内 HUD（自动任务/四挑战），不是大厅。

【问题清单】（逐条回答，找不到就写"未出现"，不许猜）
1. 选关第一轮（200601 t1312，录屏 +1563s / 20:22:50）：脚本 click:stage_target_1-12 @ [1237,841]。点下去之后，列表里高亮/选中的格子写的是几-几？同一屏可见的关卡号从几到几？是不是 1-12？
2. 同轮第二次点（t1318，+1567s，@ [1237,802]）高亮有没有换成另一格？随后 t1321 StageStart @ [1240,914]（窗口标 1600x900，Y=914 已出界）实际点到了「开始游戏」还是点空/点到别的？
3. 后两轮选关同样拍：t3060/3069（+2536s/@[1237,841] 再 @[1237,719]）和 t5157/5166（+3508s，同样 841 再 719）。三轮进局后，HUD 或关卡名显示的实际关卡是什么？
4. 主跑第一局开场（200601 tick1，+555s）已经在 MAIN_LINE，没有走选关。当时已经在打哪一关？
5. 技能三选（+561s 起，t10–t30）：三张卡分别叫什么、边框什么颜色？有没有技能点/灰掉？脚本连点「刷新」(t15/t17/t20…) 时三张有没有换？OCR 全是 name=null reason=disabled，对照画面是真灰了还是脚本认错面板？
6. 同段有没有把技能面板认成宝物？tick9 场景是 treasure_lock_btn。整份 200601 里 skill OCR 575 次、treasure OCR 3870 次，槽位 13368 次全是 disabled/spawn、零技能名。抽几帧说明当时开着的到底是 G / V / 空壳 / 别的。
7. 整晚羁绊只点了三次：t1726（+1866s）ocr_bond:敏捷祝福；t3665（+2803s）敏捷祝福；t5696（+3751s）祝福。这三帧三张卡分别是什么，点击落在哪一张上？有没有刀刀/大圣/封神出现却没被认？
8. 进房空转：200601 第一段 ROOM_STARTING t1064–t1310（+1465s 起约 96s，后半 context=UNKNOWN、零场景）。是加载、禁用卡组、英雄弹窗、选卡，还是丢窗？另外 195723 +38s–+67s：CreateRoom-confirm 之后 hwnd 变成 None，当时画面是什么窗口？

【产出要求】
1. 抽帧用 tools/video_breakdown.py，输出先落 C:\tmp\lab13_200601_frames\，筛选后有价值的帧固化进
   fixtures/lab13_200601_stage_card/，
   照现有夹具格式写 README.md + INDEX.json，focus 帧命名带时间戳（如 e_t01563.2_stage_click_1-12.jpg）。
2. 一张「问题 → 证据」表：每条问题给 帧文件路径 + trace 行号/tick + 一句话事实描述。
   只写看到了什么，不写该不该、对不对——判定归检验官。
3. trace 里顺带撞见的异常（循环点击/长时间零动作/误点）列进"顺带发现"一节，同样只报事实。
   已知日志事实（供对照，不要复述成结论）：4 局全 TIMEOUT；技能刷新 227 次、技能选中 0；进化/黑商 0；出界点击 66 次（含 G 键 [1604,882]、开始游戏 Y=914）。

【边界】
- 只读分析 + 新增 fixtures；不碰 src/、config/、tests/ 的任何行为文件。
- 不跑任何真机 bat（你的对象是已录好的素材）。
- 输出目录不要和其它拆解单撞名；同一录像已有别人在拆时先确认分工再动。
- 夹具若要提交，单独一个"感知/素材"层 commit，过 python tools/release_gate.py。
```
