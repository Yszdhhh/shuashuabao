# 抽帧单 · 板块 5 D1 丢窗（15 号 bat：连跑 4 局回房）

用户跑完 `测试夹\15-板块5-D1-连跑4局丢窗.bat` 并填好〔〕后，把下面整段复制给新会话。本单做完即弃。判定归板块 7 / 板块 5，拆解员只交证据。

本单是 D1 修代码的前置：没有回房后 KK/游戏窗的真机帧，不得改 `_capture_best`。

```text
你是一次性抽帧拆解员。工作目录：G:\刷刷宝\GameScript-Local
先读 AGENTS.md 的红线（不改行为代码、合成帧不冒充实机证据），然后只做本单，做完即收工。

【素材】
录屏：〔完整路径，如 C:\Users\10639\Desktop\录屏素材\YYYYMMDD_HHMMSS.mp4〕
对应 trace：〔%LocalAppData%\ShuaBao\日期\trace_lab_*.jsonl，4 局可能不止一个文件，按时间列全〕
incidents（若有）：〔同上〕
用户桌面补截（若丢窗时拍了）：〔完整路径或"无"〕
录屏与 trace 的对齐基准：〔录屏 00:00 ≈ 文件名时间 或 trace 第一条 tick〕

【问题清单】（逐条回答，找不到就写"未出现"，不许猜）
1. 第 1 局：选关 → 开始游戏 → 进局 → 退出 → 回到哪个窗口？（KK 房间 / 游戏选关 / 其它）房间里「开始」按钮看不看得见？
2. 第 2 局回房后点 KK 开始：游戏窗有没有出来？客户区尺寸大概多少？标题栏写的是什么？
3. 第 3 局、第 4 局同样拍：回房后、点 KK 开始后 0–5 秒、10 秒、超时前最后一帧。每一帧：KK 窗在不在、游戏窗在不在、是不是 0x0 / 看不见。
4. trace 里若出现 hwnd=None / 0x0 / unhealthy frame timeout / ROOM_STARTING 停住：对照录屏同一时刻画面是什么（黑屏、加载、KK 大厅、选关、桌面、窗被最小化）？
5. 丢窗当时：任务栏里「英雄三国 / KK」图标还在吗？窗口是被关掉、被别的窗挡住，还是还在但脚本抓不到？
6. 有没有点到「快速加入 / 快速匹配」或进了陌生房间？（C4 红线，只报画面事实）

【产出要求】
1. 抽帧用 tools/video_breakdown.py，输出先落 C:\tmp\plate5_d1_frames\，筛选后固化进
   fixtures/plate5_d1_window_loss/，照现有夹具写 README.md + INDEX.json，
   focus 帧命名带时间戳（如 e_t00612.4_room_start_hwnd_none.jpg）。
2. 一张「问题 → 证据」表：每条问题给 帧文件路径 + trace 行号/tick + 一句话事实描述。
   只写看到了什么，不写该不该、对不对——判定归检验官。
3. trace 里顺带撞见的异常（循环点击/长时间零动作/误点）列进"顺带发现"一节，同样只报事实。

【边界】
- 只读分析 + 新增 fixtures；不碰 src/、config/、tests/ 的任何行为文件。
- 不跑任何真机 bat（你的对象是已录好的素材）。
- 输出目录不要和其它拆解单撞名；同一录像已有别人在拆时先确认分工再动。
- 夹具若要提交，单独一个"感知/素材"层 commit，过 python tools/release_gate.py。
```
