# 抽帧单 · 板块 5 D5 选关 1-9（16 号 bat）

用户跑完 `测试夹\16-板块5-D5-选关1-9.bat` 并填好〔〕后，把下面整段复制给新会话。本单做完即弃。判定归板块 7 / 板块 5，拆解员只交证据。

```text
你是一次性抽帧拆解员。工作目录：G:\刷刷宝\GameScript-Local
先读 AGENTS.md 的红线（不改行为代码、合成帧不冒充实机证据），然后只做本单，做完即收工。

【素材】
录屏：〔完整路径〕
对应 trace：〔%LocalAppData%\ShuaBao\日期\trace_lab_*.jsonl〕
用户选关页补截（若开不了局拍了）：〔完整路径或"无"〕
录屏与 trace 的对齐基准：〔录屏 00:00 ≈ 文件名时间 或 trace 第一条 tick〕

【问题清单】（逐条回答，找不到就写"未出现"，不许猜）
1. 选关页可见关卡号从几-几到几-几？左边章节数字是 1 还是别的？
2. 列表里有没有一格看起来像 1-9？字形是「1-9」还是更像「1-3」？
3. 脚本点了哪一格（坐标 + 格子上写的号）？点完高亮在几-几？
4. 之后是点了底栏「开始游戏」、停在选关零动作，还是报错停机？trace 里读到的目标是 1-9 还是 1-3？
5. 若进了局：HUD / 关卡名显示的实际关卡是什么？

【产出要求】
1. 抽帧用 tools/video_breakdown.py，输出先落 C:\tmp\plate5_d5_frames\，筛选后固化进
   fixtures/plate5_d5_stage_1_9/，照现有夹具写 README.md + INDEX.json，
   focus 帧命名带时间戳（如 e_t00041.2_stage_row_1-9.jpg）。
2. 一张「问题 → 证据」表：每条问题给 帧文件路径 + trace 行号/tick + 一句话事实描述。
   只写看到了什么，不写该不该、对不对——判定归检验官。
3. trace 里顺带撞见的异常列进"顺带发现"，只报事实。

【边界】
- 只读分析 + 新增 fixtures；不碰 src/、config/、tests/ 的任何行为文件。
- 不跑任何真机 bat。
- 输出目录不要和其它拆解单撞名。
- 夹具若要提交，单独一个"感知/素材"层 commit，过 python tools/release_gate.py。
```
