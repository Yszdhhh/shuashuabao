# D0 素材治理与录屏会话映射状态（2026-08-11）

> 分支：`codex/ocr-hybrid`（基线 `7bd32b2`）
> 任务包：D0（DATA）——新录屏 session 映射 / 感知去重 / panel episode 计数 / 数据缺口表 / 扩展 manifest
> 生产代码只读：未修改 mediator/matcher/capture/InputExecutor/desktop_app/api_server/CLI

## 1. 结论

**PASS（素材治理与数量门禁，候选口径）；960×540 与 truth 复核门禁保持 FAIL（如实记录，不等同 PASS）**。

本轮完成了两段新录屏（共 89 分钟 4.09 GB）的 session 映射、SHA/感知哈希去重、1fps 全索引面板/失败/胜利事件识别、panel episode 计数与扩展标注建议。
新增候选面板（production 锚点确认、postgame 排除）：skill 25 / bond 31 / treasure 41 → 每类合计 41/49/59（数量门禁 PASS，候选口径）。
**仍 FAIL 的门禁：每类真实 960×540 ≥10（0 新增，不缩放伪造，需实机专门采集）；truth 需 O1/人工复核后才正式入库。**

## 2. 修改文件

- 新增 `fixtures/ocr_choices/D0_extended_manifest.json`（扩展素材清单/标注建议，schema 对齐 O1 cropper 输入；不动 B2 主 manifest/sources.json）
- 新增 `docs/baselines/D0_STATUS_20260811.md`（本文件）
- 新增 `docs/baselines/D0_SESSION_MAP_20260811.md`（session 映射/SHA/去重明细）
- 新增 `docs/baselines/D0_GAP_TABLE_20260811.md`（数据缺口表逐项对照 O2 门禁）

未修改：`fixtures/ocr_choices/manifest.json`、`sources.json`、`tools/*`、`models/ocr/*`（OCR-EVAL 独占）。

## 3. 实现摘要

### 3.1 素材与 session 映射

| 索引 | 源文件 | 时长 | 分辨率 | fps | SHA256 | 新 session id | 内容 |
|---|---|---|---|---|---|---|---|
| idx_a | 20260810_214444.mp4 | 00:46:18 | 1920x1080 | 30 | 2f6e6f5098834f3c… | **rec9_mijing_20260810** | 秘境局 |
| idx_b | 20260810_224848.mp4 | 00:42:48 | 1920x1080 | 30 | df68c4fd3405d6a5… | **rec10_3normal_20260810** | 三普通局 |

与既有 rec1-rec8 的对应关系：**无对应**——两段新录屏为 08-10 晚间新开录制的独立 session（见 D0_SESSION_MAP 逐项证据：SHA 全异、录制时间晚于全部既有 rec、分辨率/编码 profile 不同、内容感知哈希无近重复）。

### 3.2 采样纪律

- 以 **panel episode**（面板从出现到消失的连续静止期）为采样单位；每 episode 只取 1 张主样本（episode 首帧区间内 production anchor 得分最高帧）；
- 同一面板静止持续期间的相邻帧不进入独立样本计数（episode 内其余帧仅登记 jitter）；
- 相邻静态帧（1fps 下同面板相邻帧）不进入独立样本计数；
- 960×540 正样本未通过缩放伪造（门禁要求真实 960×540，本轮 0 新增）。

### 3.3 1fps 索引事件识别（按 kind 统计）

- 索引帧总数：idx_a 2778、idx_b 2569（合计 5347）；
- 候选面板帧（stage-1 低阈 0.62）：见 verified；production 严格确认（anchor≥0.70）：见下；
- **panel episode 总数（production 确认）**：120（skill 25 / bond 31 / treasure 41 / 未分类 23），按视频：{'a': 62, 'b': 58}；
- 失败/断线/胜利帧（production 确认）：见 D0_SESSION_MAP §5；

### 3.4 新增样本标注

每 episode 主样本逐槽位输出：name/progress 紧裁 bbox（OCR 检测框归一化坐标）、raw OCR 文本、truth（词典规范名或 unknown）。
全部标注为**建议**（`annotation_status=pending_vision_review` 或 `pending_ocr_confirm`），未声称已人工审核。

## 4. 测试与命令

本任务为素材治理（无生产代码改动），不新增单元测试；执行链如下（均为一次性分析脚本，保留在 `C:/tmp/recordings/`）：

```
PY=C:/Users/10639/Desktop/🎮 影音游戏/GameScript-Local/.venv/Scripts/python.exe
$PY C:/tmp/recordings/d0_scan.py ab        # stage-1: 5347 帧 1fps 索引扫描
$PY C:/tmp/recordings/d0_verify.py          # stage-2: production 复核候选帧
$PY C:/tmp/recordings/d0_annotate.py        # stage-3: episode 提取 + OCR 标注建议
$PY C:/tmp/recordings/d0_report.py          # 生成 docs/baselines/D0_*
```

复现结论所用原始证据全部落在 `C:/tmp/recordings/d0_work/`（scan/verified/episodes JSONL+JSON）。

## 5. 验收表（O2 门禁逐项）

| # | 门禁 | 当前(旧) | 本轮新增(候选) | 合计/状态 | 证据 |
|---|---|---|---|---|---|
| 1 | 每类 ≥30 正面板 | skill 16 / bond 18 / treasure 18 | +25/+31/+41（候选口径） | skill 41 / bond 49 / treasure 59 → **PASS（候选口径，truth 待复核）** | episodes.json / D0_extended_manifest.json |
| 2 | 每类 ≥2 采集 session | 6/5/6 | +2 新 session（rec9/rec10） | **PASS** | D0_SESSION_MAP |
| 3 | 每类真实 960×540 ≥10 | 0 | 0（不缩放伪造） | **FAIL**（需实机专门采集） | manifest stats 960x540_gap |
| 4 | 真实负面板 ≥20 进入推理 | 37 | 可选追加 | **PASS** | manifest stats / negatives 目录 |
| 5 | session 无近重复泄漏 | — | 见 §6 去重报告 | **PASS** | fixture_vs_idx_phash.json + 面板区复检 |
| 6 | 任一 session 占正样本 ≤40% | rec5 29/52=55.8% **FAIL** | 新 session 稀释后 29/149=19.5% | **PASS（候选口径）** | D0_GAP_TABLE |
| 7 | truth 全部规范名或 unknown | 17 槽非词典名（O1 处理） | 新样本全部 canonical-or-unknown | 既有 **FAIL**→O1 修复；新增 **PASS** | D0_extended_manifest.json |

## 6. 数据前后对比

| 类 | B2 结束(2026-08-10) | D0 后(含新候选) | 变化 |
|---|---|---|---|
| skill | 16 | 41 | +25 |
| bond | 18 | 49 | +31 |
| treasure | 18 | 59 | +41 |
| negative | 37 | 37（+可选候选） | 0（候选另行列出） |
| 正样本 session | 7 | 9 | +2 |
| 960×540 | 1(全部类合计) | 1 | 0（不伪造） |
| rec5 占正样本 | 55.8% | 19.5% | 稀释 36.3pp |

## 7. action ledger 差异

**0**——本任务只读生产代码，未触碰任何动作路径；无 ledger 变更。

## 8. 风险、回滚与前置条件

- 标注 truth 为 OCR+词典建议，**待 O1 联系表/人工复核**后才可进入训练池（O2 门禁 7 的前置）；
- 960×540 缺口只能靠专门实机采集（蓝图 §10 明示），本轮如实保持 FAIL；
- 两段新录屏为全桌面 1920×1080 采集（含窗口外区域），面板位置按帧内 OCR 检测框定位，未复用 1600×900 的固定 ROI；
- 回滚：全部新增文件可整体删除，不影响既有 B2 数据与生产；
- 下一任务前置：OCR-EVAL 完成 O0/O1（manifest 修复 + 紧裁 ROI + 52 面板审核）后，可将 D0_extended_manifest 作为新 session 输入 O3 数据池。

## 9. commit SHA

见提交信息（`D0:` 开头）。