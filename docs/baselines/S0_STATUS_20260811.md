# S0 长期运行安全状态机 — 状态与门禁判定（2026-08-11）

- 分支：codex/ocr-hybrid；S0 提交见 git log（"S0: " 开头）
- 施工依据：docs/plans/CODEX_N2_S0_IMPLEMENTATION_20260811.md（S0 章）、docs/NEXT_STAGE_EXECUTION_BLUEPRINT_20260811.md §8、docs/reviews/NEXT_STAGE_BLUEPRINT_REVIEW_20260811.md §7 强制顺序

## 结论

**PASS（1 项登记阻塞项）**：11 个具名测试全部通过（零新增 XFAIL）；fail+panel 两帧零选卡、恢复门闩、退出窗口后置、round deadline 不可续期、连续 3 失败熔断、cycle_num 精确停止、incident 完整；N2 三项时间门禁 waiver 复评全部达标并撤销；ledger（OCR off）diff=0。

**完成阻塞项登记（不得 XFAIL 替代）**：`missing_disconnect_modal` —— 两段录屏（idx_a 2778 + idx_b 2569 帧）+ legacy rec（986 帧）共 6333 帧扫描 gameDisconnect/retryConnect 模板 0 命中（阈值 0.70）；`disconnect_uses_disconnect_path` 用合成 matcher 证据验证路径并 PASS，但 S0 宣告"支持断线恢复"前必须补真实断线弹窗素材（G0/R0 采集），当前不得声称断线链路经真实素材验证。

## N2 waiver 复评（S0 阶段门控后，工具 tools/benchmark_hot_path.py）

| 门禁 | N2.4 | S0 | 判定 |
|---|---:|---:|---|
| changed idle MAIN_LINE ≤400ms | 467.3（FAIL waiver） | **209.5**（warm_changed P50） | **PASS → 撤销 waiver** |
| unknown_page ≤500ms | 626.6（FAIL waiver） | **408.3** | **PASS → 撤销 waiver** |
| cold preload ≤250ms | 483.4（FAIL waiver） | **184.8**（idle_hud cold P50） | **PASS → 撤销 waiver** |
| search_pixels | 26.8M（idle） | 8.9M | 下降 67% |

根因：S0 ⑧ 把 archive/boss/longzhu Fail-Closed 检查改为阶段门控（LONGZHU 阶段才查 longzhu 色相；archive/boss_entry 仅局尾窗口触发），正常中段 idle HUD 不再每 tick 全帧扫描；STRONG_FAIL/disconnect 场景 ROI 化。

## 测试

全量 unittest：**342 通过 / 0 失败 / 0 错误 / 2 expectedFailure（N2 既有 XFAIL：fail_recovery_three_frames、ticket_zero_archaeology，未新增）**。
新增 `tests/test_s0_safety_state_machine.py`（17 项）、`tests/test_incident_archiver.py`（+5 项 S0.5）、`tests/test_desktop_app.py`（+1 项 desktop_worker_writes_fail_closed_incident）。

## 遗留（S0 之外）

- 断线真实素材（见上，S0 完成阻塞项）
- 实机 panel→input P95≤800ms 与点击等待可靠性实验（R0）
- LONGZHU 状态机本体（G0 波次；当前 LONGZHU 阶段仍 Fail-Closed）
