# y3 录屏抽帧（2026-08-14）

源目录：`C:\Program Files (x86)\kkduizhan\ScreenRecord\y3`（可读，**不可写**：访问被拒绝）。

抽帧写到本目录，不是合成图。**不声称这些帧证明跟车/赌木/找房已真机通过。**

完整 seek 时间线在各 `timeline.json`。仓库只留片头 + 少量局内代表帧（全量约 231MB，已删）。源 mp4 仍在 y3。

## 结论

这批录像全是**局内刷图**（技能/羁绊三选、大秘境、胜利、退出确认）。**没有大厅房间列表。** 找房蹭车缺素材。
