# 大厅找房蹭车抽帧（2026-08-14）

源：`C:\Users\10639\Desktop\录屏素材\蹭车.mp4`（47.1s，桌面 1920×1080，30fps）。  
抽帧写到本目录，**不是** Program Files。客户区按与选关夹具相同的 `(203,84) 1600×900` 裁出。

**不是合成帧。不声称找房已可真机加入。**

## 录像实际做了什么（逐帧，不照单全收）

| t (s) | 画面 | 操作 |
|---|---|---|
| 0.0 | 已在「房间列表 99+」 | 搜索框是占位字，未输入 |
| 1.5–2.0 | 搜索框出现 `4` | 输入过滤字 |
| 3–36 | 可见行几乎全是 `4/4` + `游戏中` | 刷新键带冷却文案「刷新 Ns」（约 3–5s） |
| 36.5–37.0 | 搜索框改成 `3` | 列表先还停在 4 的结果 |
| 38.5–39.5 | 列表换成含 3 的房 | 首行 `速30` `3/4` 属性空；光标在该行 |
| 40.0 | KK 房间 `824236` / 速30 / 密码无 | 单击行进房；蓝钮是「准备」 |
| 41.0 | 四人都已准备 | 蓝钮变成「取消准备」 |
| 45–47 | EV 录屏窗挡住 | 录像停。无开局、无选关、无退出回列表 |

底栏「创建房间 / 快速加入 / 快速匹配」整段列表页都在，**一次都没点**。当负样本。

## 已确认产品规则（2026-08-14）vs 本录像

- 顶层搜 `3` 或 `4`：录像做了。这是房间名子串，不是关卡 `3-` / `4-`。
- 可设精确关卡、房间名优先：规则已定。**本录像没演示**，点进的是「速30」。
- 密码房跳过：规则已定。本房「密码无」，锁标未拍到。
- 准备后压力转移 / 局末 Boss·时光·传家宝 / 打完回列表：**本录像都没有。**
- 刷新冷却写在按钮上，不是脚本自己瞎点 3–4 秒。

## 已有模板对照

| 模板 | 列表页 | 客人房「准备」 | 结论 |
|---|---|---|---|
| `lobby/create_room` | 0.98 | 0.50 | 可当列表页只读锚。找房禁止点它 |
| `room_start` / `kk_start` / `startGameBtn` | 0.49 | **0.84** | 贴默认阈值 0.85。会把准备/取消准备认成开始游戏 |
| `lobby/roomStart`（局内棕字） | 0.45 | 0.45 | 对不上 KK 大厅钮 |
| 房间列表 Tab / 刷新 / 搜索框 / 行 / 「准备」 | 无 | 无 | 还没裁模板 |

`scenes.json` 仍无 `room_list`。`lobby_room` 只是 `room_start` 别名。

## 2026-08-14 新弹窗夹具（用户静帧，不是合成）

| 文件 | 画面 | 只许点 |
|---|---|---|
| `popup_level_unmet.png` | 平台提示 / 等级不满足房间要求 | 右侧灰色「离开」。禁止左侧「创建房间」 |
| `popup_level_unmet_leave_btn.png` | 上图「离开」按钮 ROI | 同上 |
| `popup_password.png` | 输入房间密码 | 「取消」。禁止「确定」 |
| `popup_password_cancel_btn.png` | 上图「取消」按钮 ROI | 同上 |

满房「取消」仍见 `../lobby_hitch_detail_20260814/t0046.00.png`。

特殊房用户静帧：`kk_special_room_ready_gold.png` / `kk_special_room_ready_waiting.png`。底右金色「准备」，没有「锁定」按钮。

可跑脚本在测试夹：`hitch_find_room.py`（默认 dry-run）。主工程 `live_enabled` 仍 false，未接 Mediator。
