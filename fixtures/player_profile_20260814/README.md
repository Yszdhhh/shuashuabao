# 玩家画像采集夹具（2026-08-15 真机帧）

入口：`测试夹\11-画像采集-只识别.bat`（只看） / `11-画像绑定.bat`（勾选才入库）。<br>
产出只落 `%LocalAppData%/ShuaBao/profile/`。

## 产品流（user 2026-08-15）

1. **不选就不抓。** 没勾画像绑定，不写 `first_login_*.json`。
2. **第一次登录入库：** 人点顶栏「存档」→ 装备页采战力/强化 → 技能页采 16 系等级。采集器**不代点**（顶栏旁边是退出游戏）。
3. **海报：** 绑定底 + **刚入局** TAB（装备堆叠后的进图默认盘）。打一半升级/拿卡后再抓不当画像。TAB 不代按：人一进图按开，立刻跑。现有 `ingame_tab_panel.png` 是中途帧，七格保持未采集。

## 本号对照（不是全局默认）

| 字段 | 值 | 页 |
|---|---|---|
| 装备战力 | 12295 | `archive_equipment.png` 角色头顶剑标 |
| 强化等级 | 630 | 同页角色脚下圆标 |
| 奥术箭 / 爆炎箭 / 剑气 / 普攻 | 47 / 9 / 13 / 18 | `archive_skills.png` |

战力/强化图上没有「战力」「强化」四字。全屏 OCR 会混进背包 54234/Lv.62，**无标签就 missing**，除非 ROI 单独读那两个数。

## 分辨率

贴图是聊天压缩 **1024×595**，原窗仍按 1600×900。不是合成帧。
